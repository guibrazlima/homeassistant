# ======================================================================
# 🚪 Gate Cover (Modern Template Syntax)
# ======================================================================
# Migrado de cover.yaml (legacy platform: template)
# Data: 30 Janeiro 2026
# 
# Template cover para controlo do portão
# Nota: Modern template covers têm limitações, mantém a funcionalidade básica
# ======================================================================

- cover:
  - unique_id: gate
    name: "Gate"
    device_class: gate
    open_cover:
      action: switch.turn_on
      target:
        entity_id: switch.portao_shelly
    close_cover:
      action: switch.turn_on
      target:
        entity_id: switch.portao_shelly
